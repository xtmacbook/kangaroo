
/**
 * Multisampling is based on geometric pixel coverage.
   Where MSAA begins to differ from supersampling is when the pixel shader is executed.
   And the same value for the each subsample for the pixel,but the the depth buffer will N times the size of the non-MSAA case.
 */
int main()
{
	return 0;
}
